// Configuration globale pour les tests
import dotenv from "dotenv";

// Charger les variables d'environnement de test
dotenv.config({ path: ".env.test" });

// Timeout global pour les tests (sera configuré dans jest.config.js)
